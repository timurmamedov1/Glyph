fn greet(name) {
    print("Hello, " + name + "!");
}
greet("World");

fn square(x) { return x * x; }
print(square(5));  // 25

fn max(a, b) {
    if (a > b) { return a; }
    return b;
}
print(max(10, 20));  // 20

fn doNothing() { let x = 5; }
print(doNothing());  // null
